{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CargoEmpleado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombrecargo</th>
                <td>{{ entity.nombreCargo }}</td>
            </tr>
            <tr>
                <th>Descripcioncargo</th>
                <td>{{ entity.descripcionCargo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cargoempleado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cargoempleado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
