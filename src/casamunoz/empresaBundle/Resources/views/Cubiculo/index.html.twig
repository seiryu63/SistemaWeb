{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cubiculo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numerocubiculo</th>
                <th>Estadocubiculo</th>
                <th>Cubiculodisponible</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cubiculo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numeroCubiculo }}</td>
                <td>{{ entity.estadoCubiculo }}</td>
                <td>{{ entity.cubiculoDisponible }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cubiculo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cubiculo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cubiculo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
