{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tipo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombretipocliente</th>
                <td>{{ entity.nombreTipoCliente }}</td>
            </tr>
            <tr>
                <th>Descripciontipocliente</th>
                <td>{{ entity.descripcionTipoCliente }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
