{% extends 'casamunozempresaBundle::plantilla.html.twig' %}

{% block body -%}
    <h1>Sucursal list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombresucursal</th>
                <th>Ubicacion</th>
                <th>Descripcionsucursal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('sucursal_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreSucursal }}</td>
                <td>{{ entity.ubicacion }}</td>
                <td>{{ entity.descripcionSucursal }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('sucursal_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sucursal_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('sucursal_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
